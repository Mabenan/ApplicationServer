language: cpp
sudo: required
dist: trusty
before_install:
  - sudo apt-get update
  - sudo apt-get install qt5-default 
  - sudo apt-get install qttools5-dev-tools 
compiler: c++
script:
  - cmake .
  - make
  - bin/ApplicationServerTest --platform offscreen
